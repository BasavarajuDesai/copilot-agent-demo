spring.application.name=demo
server.port=8080

# GitHub issue reporter configuration used by GlobalExceptionHandler
# Set the GitHub repository owner (user or organization)
github.owner= basavaraju.desai

# Set the GitHub repository name
github.repo= https://github.com/BasavarajuDesai/copilot-agent-demo.git

# Personal Access Token with permissions to create issues in the repo.
# You can also set this as an environment variable named GITHUB_TOKEN to avoid committing the token to source control.
GITHUB_TOKEN=
      